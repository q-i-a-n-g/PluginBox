# AGENTS.MD

## 项目目标

本仓库维护一个 Chrome 插件集合，核心交付物是 `插件盒子` 文件夹。这个文件夹应保持独立可分发：只把 `插件盒子` 发给别人，对方也能直接加载为 Chrome 扩展并使用。

## 主要工具

- `链接 提取/预览/下载`：打开 `link_tool.html`，用于粘贴 JSON 或多行链接，提取图片链接、预览、下载、导出。
- `顺序下载（线上作业）`：在作业批改页面呼出下载按钮，按页面/接口顺序快速触发下载。
- `评测`：在评测页面呼出评测按钮，自动填选配置项并运行。
- `周报工具`：打开独立的周报统计页面，保持原 `周报工具/周报工具.html` 的功能。

## 开发约定

- 仓库根目录不要保留 `README.md`；唯一 README 是 `插件盒子/README.md`。
- 需要加入新工具时，优先保持工具页面独立，再从 `插件盒子` 主入口添加链接。
- 扩展页不能使用普通内联脚本，除非作为 manifest sandbox page；普通扩展页面脚本应放到独立 `.js` 文件。
- 需要在普通网页中长期显示的悬浮按钮，应把开关状态保存到 `chrome.storage.local`，新页面自动恢复。
- 不提交 `.DS_Store`。

## 验证

修改后至少检查：

- `node --check 插件盒子/content.js`
- `node --check 插件盒子/background.js`
- `node --check 插件盒子/link_tool.js`
- `node --check 插件盒子/popup.js`
- `插件盒子/manifest.json` 可被 `JSON.parse` 解析
